# typeflow

Typeflow is a library for building typed node graphs in Vue.
It checks if pins can be connected and finds types for type variables in the graph. The library also has ready Vue components and allow to build a custom UI
